{
    // use default GitHub Codespace dev container to reduce startup time & billed storage usage
    "name": "GitHub Codespace configuration",
	"onCreateCommand": "git submodule update --init",
	"customizations": {
		"vscode": {
			"extensions": [
				"dbaeumer.vscode-eslint"
			]
		}
	}
}
